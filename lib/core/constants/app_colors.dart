import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Modern gradient palette
  static const primary = Color(0xFF667EEA);
  static const primaryDark = Color(0xFF5A67D8);
  static const primaryLight = Color(0xFF9F7AEA);
  static const secondary = Color(0xFFF093FB);
  static const accent = Color(0xFF4FD1C7);

  // Gradient Colors
  static const gradientStart = Color(0xFF667EEA);
  static const gradientEnd = Color(0xFF764BA2);
  static const cardGradientStart = Color(0xFFF093FB);
  static const cardGradientEnd = Color(0xFFF5576C);

  // Neutral Colors
  static const black = Color(0xFF2D3748);
  static const white = Color(0xFFFFFFFF);
  static const grey = Color(0xFF718096);
  static const lightGrey = Color(0xFFF7FAFC);
  static const darkGrey = Color(0xFF4A5568);

  // Background Colors
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);

  // Functional Colors
  static const success = Color(0xFF48BB78);
  static const error = Color(0xFFE53E3E);
  static const warning = Color(0xFFED8936);
  static const info = Color(0xFF3182CE);

  // Text Colors
  static const textPrimary = Color(0xFF2D3748);
  static const textSecondary = Color(0xFF718096);
  static const textLight = Color(0xFFA0AEC0);

  // Shadow Colors
  static const shadowLight = Color(0x1A000000);
  static const shadowMedium = Color(0x33000000);
}
